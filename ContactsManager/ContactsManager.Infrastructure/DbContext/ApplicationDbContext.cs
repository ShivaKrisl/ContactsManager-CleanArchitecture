namespace Db
{
    /// <summary>
    /// Legacy compatibility type kept so older tests and comments can still reference the old context name.
    /// The application now uses file-backed storage, so this class is intentionally empty.
    /// </summary>
    public class ApplicationDbContext
    {
    }
}
